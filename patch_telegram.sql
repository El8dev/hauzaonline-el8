-- ====================================================================
-- تحديث قاعدة البيانات لإضافة حساب التليجرام للطالب
-- ====================================================================

-- تشغيل هذا الاستعلام سيضيف عمود 'telegram_user' إلى جدول الطلاب
-- إذا لم يكن موجوداً مسبقاً.
ALTER TABLE public.students ADD COLUMN IF NOT EXISTS telegram_user TEXT;
